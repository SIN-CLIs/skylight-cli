# AGENTS.md — skylight-cli
**ACT** component of Stealth Triade: invisible macOS automation via SkyLight.framework.
Build: `swift build -c release && cp .build/release/skylight /usr/local/bin/skylight-cli`
Commands: `screenshot --mode som`, `click --element-index N`, `type`, `scroll`, `drag`
